# Archived M2 event / projection contracts

Frozen before M7-06 retirement of the real-time event projection line
(ADR-032). Do not treat these as live platform contracts.

Git tag: `archive/m2-event-projection` (points at the last commit that still
contained the M2 implementation tree in history).

Live integration path is incremental data ingest (Raw/ODS + export pull).
